<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.juandaniel.practicas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".conRx_Retrofit_RecyView.RRR_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".conSharedPreferences.MainActivity" />
        <activity android:name=".conRetrofit.conRetrofitActivity" />
        <activity android:name=".conRetrofit_RxJava.retrofit_rxJava"></activity>
    </application>

</manifest>